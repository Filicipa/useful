modules:
  http_2xx:
    http:
      fail_if_not_ssl: true #Or false for http
      ip_protocol_fallback: false
      method: GET
      follow_redirects: true
      preferred_ip_protocol: ip4
      valid_http_versions:
        - HTTP/1.1
        - HTTP/2.0
    prober: http
    timeout: 20s
